# Follow-through checkpoint skip target

Make the existing follow-through receipt's `#main-content` destination
programmatically focusable with `tabindex="-1"`, matching the conversion
receipt. The target stays out of normal tab order; data, privacy, and action
semantics are unchanged.
