# Plan: select valid schema summary channel

1. Add RED helper tests for warning+stdout and both-valid channels.
2. Implement minimal channel selection and run focused/static checks.
3. Refresh provenance, publish once, install exact version, and verify identity.
