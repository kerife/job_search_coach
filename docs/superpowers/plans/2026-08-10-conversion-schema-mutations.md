# Plan: cover conversion schema mutations

1. Add RED conversion mutation assertions to the schema harness test.
2. Confirm GREEN with the current schema; run focused/static tests.
3. Refresh provenance, publish once, install exact version, and verify identity.
