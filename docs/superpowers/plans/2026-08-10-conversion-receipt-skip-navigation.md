# Plan: conversion receipt skip navigation

1. Add RED assertions for a localized anchor and target in both locales.
2. Change only the template element from a paragraph to an anchor.
3. Run conversion contract/renderer tests and publish/install the increment.
