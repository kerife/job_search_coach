# Plan: conversion receipt skip target

1. Add a RED assertion for `tabindex="-1"` on the existing main target.
2. Add the attribute in the template only.
3. Run focused receipt tests and publish/install the increment.
