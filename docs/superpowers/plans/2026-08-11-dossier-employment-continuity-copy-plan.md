# Dossier Employment-Continuity Copy Guard Plan

1. Add focused RED tests for English/Spanish separation imperatives across the
   scoped market and action fields, plus neutral/negated copy acceptance.
2. Add a small shared dossier-copy guard with bounded English/Spanish patterns
   and a safe exception for explicit negated continuity boundaries.
3. Invoke the guard from dossier validation before any renderer can copy the
   text; keep schemas unchanged because this is semantic copy policy.
4. Run focused dossier validator/renderer tests, plugin tests, root suite,
   static/schema/privacy/release gates, then consume the cachebuster once.
5. Install the new Professional Growth Coach version and verify source/cache
   identity, installed/enabled state, and the final clean worktree.
