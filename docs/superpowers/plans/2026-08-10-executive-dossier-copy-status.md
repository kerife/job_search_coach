# Plan: executive dossier copy status

1. Add RED tests for status semantics, stable button labels, generic messages,
   and print hiding.
2. Update renderer labels, copy-card markup, and inline copy handler with the
   live status boundary; add minimal CSS.
3. Run focused dossier tests plus plugin static/privacy checks and inspect the
   diff.
4. Request independent review, then publish/install only after full gates pass.
