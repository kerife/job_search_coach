# Dossier Dark-Label Contrast Plan

1. Add a RED static assertion for the dossier label selector and contrast.
2. Add the dark override and synchronize `theme.md`.
3. Run focused dossier/dark/token/parity tests plus release gates.
4. Cache-bust once, install once, refresh provenance/smoke attestation, and
   rerun complete plugin and repository suites.
