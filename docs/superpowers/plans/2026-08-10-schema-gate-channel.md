# Plan: test schema-gate summary channels

1. Add RED test for stdout-only summary and stderr precedence.
2. Apply the minimal assertion alignment and run focused/full gate checks.
3. Refresh provenance, publish once, install exact version, and verify identity.
