# Plan: test static gate abort behavior

1. Add RED in-process tests for timeout and invalid-summary main branches.
2. Implement only the minimal early-return wiring needed and run focused tests.
3. Refresh provenance, publish once, install exact version, and verify identity.
