# Plan: executive dossier confirmation boundary

1. Add RED assertions for localized confirmation descriptions and
   `aria-describedby` references.
2. Add the fixed description only to `requires_confirmation` copy cards.
3. Run focused dossier/privacy tests, inspect the diff, and publish/install the
   increment with the normal provenance and identity gates.
