# Plan: align nonzero diagnostic channels

1. Add RED helper tests for warning/error channel combinations.
2. Reuse the shared selector with minimal code and run focused/static checks.
3. Refresh provenance, publish once, install exact version, and verify identity.
