# Practice Confirm Dark-Contrast Plan

1. Add RED assertions for the confirm selector, dark colors, and contrast
   ratio; add EN/ES render coverage if missing.
2. Add the screen-only CSS override and synchronize the practice raw dump in
   `.superdesign/init/theme.md`.
3. Run focused contrast/render/theme/design-token tests and full gates.
4. Cache-bust once, install once, verify source/cache parity and smoke, then
   refresh provenance and attestation.
