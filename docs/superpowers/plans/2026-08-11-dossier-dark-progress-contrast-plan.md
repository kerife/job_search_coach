# Dossier Dark Progress Contrast Plan

1. Add a RED static test for dark progress track selectors and token contrast.
2. Add the minimal dark CSS override and synchronize the Superdesign dump.
3. Run dark-mode, dossier renderer, parity, static, and plugin tests.
4. Bump once, install, refresh provenance/attestation, compare source/cache, and
   run installed smoke plus release validation.
